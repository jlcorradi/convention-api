logging.level.dev.jlcorradi.convention: TRACE

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/convention?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: s3cr3t
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
